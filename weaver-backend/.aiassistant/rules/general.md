---
apply: always
---

This is a Java 25 Spring Boot 4 project for a kanban-inspired project management system.

## Rules for Development
1. Use Java 25 LTS features.
2. Do not try to compile and test unless I ask you to.
3. Use the `dev.jamius.weaver.dto.ApiResponse` class for API responses.

## Tools
1. Check if `rg` is available, if so use it, otherwise use `grep`.